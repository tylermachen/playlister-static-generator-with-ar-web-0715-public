<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= song.name %></title>
</head>
<body>
    <a href="../artists/<%= @song.artist.to_slug %>.html"><%= @song.artist.name %></a>
    <a href="../genres/<%= @song.genre.to_slug %>.html"><%= @song.genre.name %></a>
</body>
</html>
